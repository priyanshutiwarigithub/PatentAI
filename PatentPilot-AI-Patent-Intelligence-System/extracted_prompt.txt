PatentMind AIEnterprise Patent Intelligence PlatformBig Data · RAG · Large Language ModelsAntigravity Build KitAGENTS.md  ·  Phase-wise Prompts  ·  Architecture DecisionsRepository: github.com/Omkar210/PatentPilot-AI-Patent-Intelligence-SystemLocked Architecture DecisionsAll decisions below are final. Do not change these during Antigravity tasks unless this document is explicitly updated first.LayerDecision + FallbackLLM (primary)Qwen3-4B via Ollama — GPU server onlyLLM (fallback)Groq API (llama-3.3-70b-versatile) — if Ollama unavailableOCR (primary)PaddleOCR PP-OCRv4 + PP-StructureV2 (batch ingestion, GPU)OCR (fallback)PyMuPDF text layer — for digitally generated PDFs (no scan needed)Vector DB (primary)Qdrant — single binary, uploaded to GPU serverVector DB (fallback)ChromaDB — pip-installable Python fallback, auto-switch on Qdrant failureFrontendReact + Tailwind CSSBackendFastAPI + LangGraphRelational DBPostgreSQL via SQLAlchemy 2.0 + Alembic migrationsPDF StorageAmazon S3 — original patent PDFsEmbeddingssentence-transformers all-MiniLM-L6-v2 (GPU-accelerated, ~1GB VRAM)Graph DBNeo4j community editionData SourcesUSPTO PatentsView API, WIPO API, Google Patents (SerpAPI or BigQuery)Target volumeMinimum 200 unique patents, AI/ML/LLM/CV/NLP domains onlyVRAM Budget (20 GB GPU Server)ComponentVRAM allocationQwen3-4B via Ollama (Q4 quantized)~4 GBPaddleOCR PP-OCRv4 (batch, not concurrent with Qwen)~2 GBEmbedding model (sentence-transformers)~1 GBQdrant (CPU — no GPU needed)0 GBSafe headroom~13 GB remainingRuleNever run PaddleOCR and Qwen3-4B simultaneously — sequence themAGENTS.mdSave this file at the repository root. Antigravity reads it automatically at the start of every task. It replaces the need to re-paste project context into every prompt.# PatentMind AI — Agent Rules## ProjectEnterprise patent intelligence platform. Big Data + RAG + LLM.Collects, processes, and semantically indexes 200+ AI/ML patents.Enables natural language querying via a RAG pipeline and Qwen3-4B.## Locked 15-stage workflow (do not modify without being asked)1.  Patent Extraction          — USPTO, WIPO, Google Patents APIs2.  Duplicate Detection        — cross-source dedup by patent number3.  Metadata Validation        — required fields check before storage4.  S3 Storage                 — original PDFs to Amazon S35.  PDF Text Extraction        — PyMuPDF for text-layer PDFs6.  OCR Processing             — PaddleOCR for scanned pages (batch, GPU)7.  Patent Text Cleaning       — noise, headers, footers removed8.  Metadata Enrichment        — IPC/CPC normalisation, inventor dedup9.  Patent Chunking            — section-aware + claim-aware chunking10. Embedding Generation       — sentence-transformers, GPU batch11. Vector DB Storage          — Qdrant primary, ChromaDB fallback12. Semantic Retrieval         — query embedding + hybrid search13. Context Generation         — top-k chunks assembled with metadata14. LLM Answer Generation      — Qwen3-4B via Ollama; Groq fallback15. User Interface             — React + Tailwind, FastAPI backend## GPU server rules- OCR (PaddleOCR) runs as a batch ingestion job — NEVER at query time.- Qwen3-4B loads for query serving — do not load during OCR batch.- Never run both simultaneously — OOM risk on 20GB VRAM.- Embedding generation runs as a separate batch after OCR is complete.## Tech stack (exact versions — do not upgrade without asking)fastapi==0.116.1, uvicorn==0.35.0, sqlalchemy==2.0.43,psycopg2-binary==2.9.10, alembic==1.16.5, langgraph==0.6.6,langchain==0.3.27, langchain-community==0.3.29, langchain-core==0.3.75,chromadb==1.0.20, neo4j==5.28.2, networkx==3.5,sentence-transformers==5.1.0, pandas==2.3.2, numpy==2.3.2,httpx==0.28.1, requests==2.32.5, PyMuPDF==1.26.4,paddlepaddle==3.1.1, paddleocr==3.2.0, opencv-python==4.12.0.88,Pillow==11.3.0, transformers==4.55.4, accelerate==1.10.0,groq==0.31.0## Coding rules- Every module goes in its designated folder (see folder structure below).- All API keys via python-dotenv from .env — never hardcoded.- New required env vars must be added to .env.example.- Qdrant client must wrap every call in a try/except that falls back  to ChromaDB automatically — this is the mandatory fallback pattern.- Qwen3-4B calls must wrap in try/except that falls back to Groq API.- OCR pipeline must check PyMuPDF text layer first; run PaddleOCR only  if extracted text length < 100 characters per page.- Write a test for every new function before marking a task done.- Use the browser agent to verify FastAPI /docs renders after every  new endpoint is added.- Commit with clear messages per sub-task. No bundled unrelated changes.## Folder structurepatentmind/├── ingestion/          ← patent extraction clients (USPTO, WIPO, Google)├── processing/         ← OCR, PDF text extraction, cleaning, chunking├── embeddings/         ← embedding generation, vector DB client├── retrieval/          ← RAG pipeline, context assembly├── llm/                ← Qwen3-4B Ollama client + Groq fallback├── api/                ← FastAPI routes├── db/                 ← SQLAlchemy models, Alembic migrations, session├── storage/            ← S3 client wrapper├── graph/              ← Neo4j client├── frontend/           ← React + Tailwind app├── tests/              ← per-module tests├── docs/               ← architecture diagrams, design decisions├── .env.example├── docker-compose.yml└── AGENTS.mdPhase 0  Project Setup & EnvironmentObjectiveBootstrap the repository, install dependencies, configure environment variables, and verify all services start correctly before any agent code is written.Prompt — paste into Antigravity Planning modeSet up the PatentMind AI project from scratch.1. Create the folder structure defined in AGENTS.md at the repo root.2. Create requirements.txt with these exact pinned versions:   fastapi==0.116.1, uvicorn[standard]==0.35.0, sqlalchemy==2.0.43,   psycopg2-binary==2.9.10, alembic==1.16.5, python-dotenv==1.1.1,   python-multipart==0.0.20, langgraph==0.6.6, langchain==0.3.27,   langchain-community==0.3.29, langchain-core==0.3.75,   chromadb==1.0.20, neo4j==5.28.2, networkx==3.5,   sentence-transformers==5.1.0, pandas==2.3.2, numpy==2.3.2,   httpx==0.28.1, requests==2.32.5, PyMuPDF==1.26.4,   paddlepaddle==3.1.1, paddleocr==3.2.0, opencv-python==4.12.0.88,   Pillow==11.3.0, transformers==4.55.4, accelerate==1.10.0,   boto3==1.35.0, qdrant-client==1.9.1, groq==0.31.0, tqdm==4.67.1,   rich==14.1.03. Create .env.example with all required environment variables:   DATABASE_URL, AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY,   AWS_REGION, S3_BUCKET_NAME, QDRANT_HOST, QDRANT_PORT,   NEO4J_URI, NEO4J_USER, NEO4J_PASSWORD,   GROQ_API_KEY, OLLAMA_BASE_URL,   USPTO_API_KEY, SERPAPI_KEY.4. Create docker-compose.yml with services for:   - PostgreSQL 15 (port 5432, named volume for persistence)   - Neo4j community (ports 7474 browser, 7687 bolt, named volume)   Do NOT add Qdrant to Docker — it runs as an uploaded binary on   the GPU server. Do NOT add ChromaDB — it uses embedded/persistent   client with a local folder.5. Install requirements.txt in a virtual environment and confirm   every package imports cleanly. Flag any version conflict.6. Start docker-compose services and confirm PostgreSQL and Neo4j   are reachable on their ports. Show the output.Use Planning mode. Show the plan before executing.Phase 1  Database Schema & S3 Storage LayerObjectiveBuild the PostgreSQL persistence layer (ORM models + migrations) and the S3 client wrapper before any ingestion code runs — everything downstream stores data here.Prompt — paste into Antigravity Planning modeBuild the database and storage layer for PatentMind AI.1. In /db, create SQLAlchemy 2.0 ORM models for these tables:   patents — patent_id (PK), patent_number (unique), title, abstract,     claims (text), description (text), inventors (JSONB), assignee,     filing_date, publication_date, cpc_codes (JSONB), ipc_codes (JSONB),     pdf_url, s3_key, source_repository, domain_tags (JSONB),     processing_status, created_at, updated_at.   processing_logs — id, patent_id (FK), stage, status, error_message,     started_at, completed_at.   embeddings_meta — id, patent_id (FK), chunk_index, chunk_text,     vector_db_id, created_at.2. Set up Alembic pointed at DATABASE_URL from .env.   Generate and apply the initial migration.   Verify all tables exist in PostgreSQL.3. Create db/session.py with a SQLAlchemy 2.0 AsyncSession factory   using psycopg2-binary. Expose get_db() as a FastAPI dependency.4. In /storage, create s3_client.py wrapping boto3:   - upload_patent_pdf(patent_number, file_bytes) -> s3_key   - download_patent_pdf(s3_key) -> bytes   - check_exists(s3_key) -> bool   Use AWS credentials from .env. Add retry logic (3 attempts,   exponential backoff). Log every upload with rich.5. Write tests in /tests:   - Insert one dummy patent row and read it back.   - Upload a small dummy PDF to S3 and verify it exists.   Run both tests and show output.Use Planning mode.Phase 2  Patent Data IngestionObjectiveExtract 200+ unique patents from USPTO, WIPO, and Google Patents across the target AI/ML domains. Deduplicate, validate, store PDFs to S3, and persist metadata to PostgreSQL.Prompt — paste into Antigravity Planning modeImplement patent ingestion for PatentMind AI.Target domains (only these — no others):Artificial Intelligence, Machine Learning, Deep Learning, LLMs,Agentic AI, RAG, Computer Vision, NLP, Generative AI,AI Infrastructure, Vector Databases, Edge AI, Robotics,Autonomous Systems, Emerging Technologies.1. In /ingestion, build three async API clients using httpx:   a) uspto_client.py — PatentsView API      Search by CPC codes covering AI/ML (G06N, G06V, G10L, etc.).      Retrieve: patent_number, title, abstract, inventors, assignee,      filing_date, publication_date, cpc_codes, pdf_url.      Handle pagination to collect 100+ patents.      Rate limit: max 45 requests/min. Use exponential backoff on 429.   b) wipo_client.py — WIPO PatentScope API      Search IPC codes covering AI/ML domains.      Map WIPO response fields to the same schema as USPTO above.      Target 60+ patents.   c) google_patents_client.py — SerpAPI Google Patents endpoint      Use SERPAPI_KEY from .env.      Search for AI/ML patent queries.      Map response to the shared schema.      Target 40+ patents.2. In /ingestion/pipeline.py, build the ingestion pipeline:   Step 1: Run all three clients concurrently via asyncio.gather.   Step 2: Deduplicate by patent_number across sources — keep first           occurrence, log duplicates removed.   Step 3: Validate required fields (patent_number, title, abstract,           pdf_url must be non-empty). Log and skip invalid records.   Step 4: For each valid patent, download the PDF from pdf_url           and upload to S3 via storage/s3_client.py. Store the           returned s3_key on the patent record.   Step 5: Persist all valid patents to PostgreSQL patents table.           Set processing_status = "ingested".   Step 6: Print a rich summary table: total fetched per source,           duplicates removed, validation failures, final count stored.3. Run the pipeline against real APIs. Show the rich summary.   Confirm minimum 200 unique patents are stored in PostgreSQL.4. Write a test in /tests that mocks the three API clients and   verifies dedup + validation logic works correctly.Use Planning mode. Use the browser agent to check PatentsView,WIPO PatentScope, and SerpAPI documentation for exact endpointsand response schemas before writing client code.Phase 3  Document Processing — OCR, Cleaning & ChunkingObjectiveProcess all ingested patent PDFs: extract text (PyMuPDF first, PaddleOCR fallback), clean the output, enrich metadata, then chunk patent content using a section-aware and claim-aware strategy.Critical GPU ruleRun this entire phase as a batch job via SSH on the GPU server. Do not load Qwen3-4B during this phase. PaddleOCR uses ~2GB VRAM — leave it free for serving later.Prompt — paste into Antigravity Planning modeImplement document processing for PatentMind AI.1. In /processing/pdf_extractor.py:   Use PyMuPDF (fitz) to extract text from each page.   If extracted text for a page is < 100 characters, flag that page   as "scanned" — it needs OCR.   Return: {page_num, text, is_scanned} for each page.2. In /processing/ocr_engine.py:   Load PaddleOCR once at module level:   PaddleOCR(use_angle_cls=True, lang="en", use_gpu=True,             structure_version="PP-StructureV2")   For scanned pages: render the page to an image via PyMuPDF   (mat = fitz.Matrix(2, 2) for 2x resolution), then run OCR.   For tables detected: use PP-StructureV2 table extraction.   Return: plain text string per page.   IMPORTANT: load the OCR engine ONCE for the batch — never   per-document or per-page. Log pages processed with tqdm.3. In /processing/cleaner.py:   Remove headers and footers (repeated lines across pages).   Remove page numbers, watermarks, and OCR artefacts.   Normalise whitespace and hyphens from line wrapping.   Preserve section headings (CLAIMS, ABSTRACT, DESCRIPTION,   BACKGROUND, SUMMARY, BRIEF DESCRIPTION OF DRAWINGS).4. In /processing/chunker.py — section-aware + claim-aware strategy:   a) Split patent text into named sections using the headings above.   b) For the CLAIMS section: split each numbered claim into its own      chunk. Never break a claim across chunks.   c) For all other sections: apply recursive chunking at      chunk_size=512 tokens, overlap=64 tokens.   d) Each chunk must carry metadata: patent_number, section_name,      claim_number (if applicable), chunk_index, source_s3_key.5. In /processing/pipeline.py:   Fetch all patents with processing_status = "ingested" from DB.   For each patent:     - Download PDF from S3.     - Extract text (PyMuPDF + PaddleOCR fallback per page).     - Clean text.     - Enrich: normalise IPC/CPC codes, deduplicate inventor names.     - Chunk text.     - Store chunks to DB embeddings_meta table with chunk_text.     - Update processing_status = "processed".   Log errors per patent to processing_logs table.   Run with a tqdm progress bar. Show final rich summary.6. Run the pipeline on the GPU server via SSH on the 200+ patents.   Show tqdm progress and final summary. Confirm processing_status   updated to "processed" in PostgreSQL for all patents.Use Planning mode.Phase 4  Embedding Generation & Vector StorageObjectiveGenerate embeddings for all patent chunks and store them in Qdrant (primary) with automatic ChromaDB fallback. Confirm semantic retrieval returns meaningful results before building the RAG layer.Prompt — paste into Antigravity Planning modeImplement embedding generation and vector storage for PatentMind AI.1. In /embeddings/encoder.py:   Load SentenceTransformer("all-MiniLM-L6-v2") with device="cuda".   Expose batch_encode(texts: List[str]) -> List[List[float]].   Use batch_size=64 for GPU efficiency.   Log progress with tqdm.2. In /embeddings/vector_store.py, implement the dual-backend client:   class VectorStore:     __init__: try to connect to Qdrant at QDRANT_HOST:QDRANT_PORT.       On connection failure, log warning and fall back to ChromaDB       PersistentClient at ./chroma_data. Set self.backend accordingly.     upsert(chunks: List[dict], embeddings: List[List[float]]):       For Qdrant: upsert to collection "patent_chunks" with         payload = {patent_number, section_name, claim_number,                    chunk_index, chunk_text, source_s3_key}.       For ChromaDB: add to collection "patent_chunks" with         documents=chunk_text, metadatas=payload, ids=unique_ids.     search(query_embedding, top_k=5, filters=None):       Qdrant: use query_points with optional payload filter.       ChromaDB: use collection.query with where clause.       Return unified format: [{chunk_text, metadata, score}].     CRITICAL: every Qdrant call must be in try/except — on any     QdrantException, switch self.backend to ChromaDB and retry.3. In /embeddings/pipeline.py:   Fetch all chunks from DB with processing_status = "processed".   Batch encode with encoder.py.   Upsert to VectorStore.   Update processing_status = "embedded" in PostgreSQL.   Show rich summary: chunks embedded, backend used, time taken.4. Run on the GPU server. Confirm:   - All 200+ patents have chunks embedded.   - A test query ("transformer attention mechanism patent") returns     the top 5 semantically relevant chunks.   - Show which backend was used (Qdrant or ChromaDB).5. Write a test that runs a sample search against real data and   asserts all returned results contain the metadata fields.Use Planning mode.Phase 5  RAG Pipeline & LLM IntegrationObjectiveBuild the complete RAG pipeline: query preprocessing, semantic retrieval, context assembly, and answer generation via Qwen3-4B (Ollama). Wire Groq API as automatic fallback.Prompt — paste into Antigravity Planning modeImplement the RAG pipeline and LLM integration for PatentMind AI.1. In /llm/ollama_client.py:   Wrap the Ollama REST API (OLLAMA_BASE_URL from .env).   Model: qwen3:4b   Parameters: temperature=0.1, top_p=0.9, num_ctx=8192,     num_predict=1024.   Expose generate(prompt: str) -> str.   On any connection error or timeout: raise OllamaUnavailableError.2. In /llm/groq_client.py:   Use groq SDK. Model: llama-3.3-70b-versatile.   Temperature=0.1, max_tokens=1024.   Expose generate(prompt: str) -> str.3. In /llm/router.py:   try ollama_client.generate() first.   On OllamaUnavailableError: log warning, call groq_client.generate().   Log which backend was used for every call.4. In /retrieval/rag_pipeline.py:   a) Query preprocessing:      Lowercase, strip noise, extract key technical terms.   b) If a research paper PDF is uploaded:      Extract text via PyMuPDF, clean it, encode it,      and prepend as additional context to the query.   c) Semantic retrieval:      Encode query via embeddings/encoder.py.      Search VectorStore top_k=8.      Optionally filter by section_name="CLAIMS" if the query      mentions "claims" or "prior art".   d) Context assembly:      Concatenate retrieved chunks with their metadata headers:      "[Patent {patent_number} | {section_name}]\n{chunk_text}".      Trim total context to 6000 tokens.   e) Prompt construction:      System: "You are a patent intelligence analyst. Answer using      only the patent context provided. Cite the patent number for      every claim you make. If the context is insufficient, say so.      Do not hallucinate patent details."      User: "{context}\n\nQuestion: {query}"   f) Call llm/router.py generate().   g) Return: {answer, sources: [{patent_number, section, score}],              llm_backend_used}.5. Test the full pipeline end-to-end:   Query: "What methods exist for training large language models   with reduced memory usage?"   Show: answer, top 3 cited patent numbers, backend used.   Verify the answer cites specific patents from the retrieved context   and does not introduce information not in the chunks.Use Planning mode.Phase 6  FastAPI BackendObjectiveExpose the full RAG pipeline via a documented REST API. All endpoints consumed by the React frontend.Prompt — paste into Antigravity Planning modeBuild the FastAPI backend for PatentMind AI.1. In /api, build the FastAPI app with these endpoints:   POST /api/query     Body: {query: str, filters?: {date_from, date_to, cpc_code}}     Runs full RAG pipeline.     Returns: {answer, sources, llm_backend_used, query_id}.   POST /api/query-with-paper     Multipart: query (str) + paper (PDF file upload).     Processes paper + runs RAG pipeline with paper as extra context.     Returns same shape as /api/query.   GET /api/patents     Query params: page, page_size, domain, source, date_from, date_to.     Returns paginated list of patent metadata from PostgreSQL.   GET /api/patents/{patent_number}     Returns full metadata for one patent.   GET /api/stats     Returns: total patents, patents per source, patents per domain,     processing_status counts, vector backend currently active.2. Add structured logging (Python logging module) to every endpoint:   log request, latency, llm_backend_used, error if any.3. Add CORS middleware permitting the React frontend origin.4. Use the browser agent: start uvicorn, open /docs, verify all   endpoints appear with correct request/response schemas.   Then call POST /api/query through /docs with a real query   and confirm a full answer is returned.5. Write tests for each endpoint using FastAPI TestClient.   Mock the RAG pipeline so tests do not call Ollama or Qdrant.Use Planning mode.Phase 7  React FrontendObjectiveBuild the React + Tailwind user interface: patent search, PDF upload, results display, and answer viewer with source citations.Prompt — paste into Antigravity Planning modeBuild the React + Tailwind frontend for PatentMind AI.1. Scaffold a React app in /frontend with Tailwind CSS configured.2. Build these screens / components:   a) Home / Query screen:      - Large text area for natural language patent query.      - PDF upload input (for research paper comparison).      - Filter panel: date range, CPC code, source repository.      - Submit button — calls POST /api/query or /api/query-with-paper.      - Loading state showing "Retrieving patents..." while waiting.   b) Results screen (shown after query returns):      - LLM answer displayed prominently with markdown rendering.      - "Sources used" section: list of cited patents with:          patent number, title, section, similarity score.      - Each source card is expandable to show the chunk text used.      - Badge showing which LLM backend answered (Qwen3-4B / Groq).   c) Patent Browser screen:      - Paginated table of all patents from GET /api/patents.      - Filter by source, domain, date.      - Click a row to open patent detail view (metadata + abstract).   d) Stats screen:      - Cards showing total patents, source breakdown, domain breakdown.      - Processing status breakdown.      - Vector DB backend currently active.3. Wire all API calls through a single api.js client module   (BASE_URL from environment variable).4. Use the browser agent to verify:   - All four screens render without console errors.   - A real query through the Home screen returns results.   - A PDF upload query works end to end.5. Build the React app to /frontend/dist (static files).   Configure FastAPI to serve /frontend/dist as StaticFiles   so the full stack runs from a single uvicorn process —   no separate web server needed on the SSH GPU server.Use Planning mode.Phase 8  Integration, Testing & DeploymentObjectiveEnd-to-end integration test, Docker Compose final setup, SSH deployment checklist, and a full demo dry-run with a real query.Prompt — paste into Antigravity Planning modeFinalize, test, and deploy PatentMind AI.1. Run an end-to-end integration test covering all 15 workflow stages:   Stage 1-4: confirm 200+ patents in PostgreSQL and S3.   Stage 5-8: confirm all patents have processing_status = "processed".   Stage 9-11: confirm all chunks have processing_status = "embedded",     confirm VectorStore search returns results.   Stage 12-14: run POST /api/query with a real query. Confirm     answer, sources, and llm_backend_used are all populated.   Stage 15: open the React UI via browser agent, submit a query,     confirm results render correctly.2. Test the Qdrant → ChromaDB fallback:   Stop the Qdrant binary. Run a query. Confirm ChromaDB fallback   activates automatically and returns results. Restart Qdrant.3. Test the Ollama → Groq fallback:   Stop Ollama. Run a query. Confirm Groq fallback activates.   Restart Ollama.4. Finalize docker-compose.yml:   Include PostgreSQL and Neo4j services.   Add the FastAPI+React backend as a service (uvicorn serving both   API and static frontend files).   Do not add Qdrant to Docker — it runs as a binary on the server.   Confirm docker-compose up --build starts all services cleanly.5. Create an SSH deployment checklist in /docs/DEPLOY.md:   - How to upload the Qdrant binary via SCP and start it.   - How to start Ollama and pull qwen3:4b.   - How to run the ingestion pipeline (Phase 2) batch job.   - How to run the processing pipeline (Phase 3) batch job.   - How to run the embedding pipeline (Phase 4) batch job.   - How to start the FastAPI server.   - How to tunnel port 8000 locally via SSH for demo access.6. Write README.md at repo root covering:   Project overview, architecture summary, setup, docker-compose   instructions, SSH deployment reference, and a demo query example.7. Do a full dry-run demo:   Query: "What are the most novel methods for training vision   transformers on edge devices described in recent AI patents?"   Use the browser agent to walk through the full UI flow.   Screenshot or describe every screen and confirm nothing breaks.Use Planning mode.Quick ReferencePhase SequencePhaseWhat gets built0 — SetupRepo, venv, Docker Compose (Postgres + Neo4j), .env.example1 — Database & S3SQLAlchemy models, Alembic migrations, S3 client2 — IngestionUSPTO + WIPO + Google Patents clients, dedup, S3 upload3 — ProcessingPyMuPDF + PaddleOCR, cleaner, chunker (batch GPU job)4 — Embeddingssentence-transformers, Qdrant + ChromaDB fallback, batch GPU5 — RAG + LLMRAG pipeline, Qwen3-4B Ollama + Groq fallback6 — APIFastAPI endpoints, CORS, logging, OpenAPI docs7 — FrontendReact + Tailwind: query, results, browser, stats screens8 — Deploy & TestE2E tests, fallback tests, SSH deploy checklist, dry-run demoKey Rules — Never Break TheseNever run PaddleOCR and Qwen3-4B simultaneously on the GPU server.OCR is a batch preprocessing step — never triggered by a user query.Every Qdrant call must auto-fall-back to ChromaDB on any exception.Every Ollama call must auto-fall-back to Groq API on any exception.All API keys in .env — never hardcoded anywhere in the codebase.Write one test per function before marking any task done in Antigravity.Run the browser agent after every new FastAPI endpoint to verify /docs.